[package]
name = "secure-vault"
version = "0.1.0"
description = "Teaching example: Secure Agent Vault with best practices"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "secure_vault"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.30"
